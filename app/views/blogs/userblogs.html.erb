
<% if @blogs.count == 0 %>
<h1>no blogs avilable</h1>
<% else %>
 <% @blogs.all.each do |blog| %>
     <h2class="card-title"><%= blog.title %></h2class=>
     <%= link_to blog.title,blog_path(blog)%>
 <% end %>
<% end %>